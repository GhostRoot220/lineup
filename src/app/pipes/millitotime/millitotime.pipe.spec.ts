import { MillitotimePipe } from './millitotime.pipe';

describe('AgotimePipe', () => {
  it('create an instance', () => {
    const pipe = new MillitotimePipe();
    expect(pipe).toBeTruthy();
  });
});
