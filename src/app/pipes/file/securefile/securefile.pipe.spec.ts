import { SecurefilePipe } from './securefile.pipe';

describe('SecurefilePipe', () => {
  it('create an instance', () => {
    const pipe = new SecurefilePipe();
    expect(pipe).toBeTruthy();
  });
});
