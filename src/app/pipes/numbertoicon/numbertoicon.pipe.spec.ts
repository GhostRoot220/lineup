import { NumbertoiconPipe } from './numbertoicon.pipe';

describe('NumbertoiconPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbertoiconPipe();
    expect(pipe).toBeTruthy();
  });
});
